{
  "name": "examroute",
  "version": "1.0.0",
  "description": "Ride-pooling platform for exam-goers (JEE/NEET/CUET). MERN + JWT + Google OAuth + Razorpay + Google Maps routing.",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed",
    "seed:demo": "npm --prefix server run seed:demo",
    "build": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "author": "",
  "license": "MIT"
}
